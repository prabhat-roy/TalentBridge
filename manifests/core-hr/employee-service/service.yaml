apiVersion: v1
kind: Service
metadata: { name: employee-service, namespace: talentbridge-core-hr }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50200, targetPort: 50200 }]
  selector: { app.kubernetes.io/name: employee-service }
