apiVersion: v1
kind: Service
metadata:
  name: knowledge-transfer
  namespace: talentbridge
  labels: { app.kubernetes.io/name: knowledge-transfer, talentbridge.io/domain: offboarding }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50242, targetPort: 50242 }]
  selector: { app.kubernetes.io/name: knowledge-transfer }
