apiVersion: apps/v1
kind: Deployment
metadata:
  name: exit-service
  namespace: talentbridge
  labels:
    app.kubernetes.io/name: exit-service
    app.kubernetes.io/part-of: talentbridge
    talentbridge.io/domain: offboarding
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: exit-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: exit-service, talentbridge.io/domain: offboarding }
    spec:
      serviceAccountName: exit-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: exit-service
          image: harbor.talentbridge.internal/talentbridge/exit-service:latest
          ports: [{ containerPort: 50240, name: http }]
          envFrom: [{ configMapRef: { name: exit-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50240 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50240 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
