apiVersion: v1
kind: Service
metadata:
  name: exit-service
  namespace: talentbridge
  labels: { app.kubernetes.io/name: exit-service, talentbridge.io/domain: offboarding }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50240, targetPort: 50240 }]
  selector: { app.kubernetes.io/name: exit-service }
