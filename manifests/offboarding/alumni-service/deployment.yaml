apiVersion: apps/v1
kind: Deployment
metadata:
  name: alumni-service
  namespace: talentbridge
  labels:
    app.kubernetes.io/name: alumni-service
    app.kubernetes.io/part-of: talentbridge
    talentbridge.io/domain: offboarding
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: alumni-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: alumni-service, talentbridge.io/domain: offboarding }
    spec:
      serviceAccountName: alumni-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: alumni-service
          image: harbor.talentbridge.internal/talentbridge/alumni-service:latest
          ports: [{ containerPort: 50243, name: http }]
          envFrom: [{ configMapRef: { name: alumni-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50243 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50243 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
