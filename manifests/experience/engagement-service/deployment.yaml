apiVersion: apps/v1
kind: Deployment
metadata:
  name: engagement-service
  namespace: talentbridge
  labels:
    app.kubernetes.io/name: engagement-service
    app.kubernetes.io/part-of: talentbridge
    talentbridge.io/domain: experience
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: engagement-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: engagement-service, talentbridge.io/domain: experience }
    spec:
      serviceAccountName: engagement-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: engagement-service
          image: harbor.talentbridge.internal/talentbridge/engagement-service:latest
          ports: [{ containerPort: 50220, name: http }]
          envFrom: [{ configMapRef: { name: engagement-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50220 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50220 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
