apiVersion: v1
kind: Service
metadata:
  name: engagement-service
  namespace: talentbridge
  labels: { app.kubernetes.io/name: engagement-service, talentbridge.io/domain: experience }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50220, targetPort: 50220 }]
  selector: { app.kubernetes.io/name: engagement-service }
