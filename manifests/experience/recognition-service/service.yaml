apiVersion: v1
kind: Service
metadata:
  name: recognition-service
  namespace: talentbridge
  labels: { app.kubernetes.io/name: recognition-service, talentbridge.io/domain: experience }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50222, targetPort: 50222 }]
  selector: { app.kubernetes.io/name: recognition-service }
