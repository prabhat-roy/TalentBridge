apiVersion: apps/v1
kind: Deployment
metadata:
  name: recognition-service
  namespace: talentbridge
  labels:
    app.kubernetes.io/name: recognition-service
    app.kubernetes.io/part-of: talentbridge
    talentbridge.io/domain: experience
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: recognition-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: recognition-service, talentbridge.io/domain: experience }
    spec:
      serviceAccountName: recognition-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: recognition-service
          image: harbor.talentbridge.internal/talentbridge/recognition-service:latest
          ports: [{ containerPort: 50222, name: http }]
          envFrom: [{ configMapRef: { name: recognition-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50222 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50222 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
