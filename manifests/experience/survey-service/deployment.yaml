apiVersion: apps/v1
kind: Deployment
metadata:
  name: survey-service
  namespace: talentbridge
  labels:
    app.kubernetes.io/name: survey-service
    app.kubernetes.io/part-of: talentbridge
    talentbridge.io/domain: experience
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: survey-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: survey-service, talentbridge.io/domain: experience }
    spec:
      serviceAccountName: survey-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: survey-service
          image: harbor.talentbridge.internal/talentbridge/survey-service:latest
          ports: [{ containerPort: 50221, name: http }]
          envFrom: [{ configMapRef: { name: survey-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50221 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50221 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
