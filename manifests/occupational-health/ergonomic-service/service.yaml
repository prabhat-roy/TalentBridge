apiVersion: v1
kind: Service
metadata:
  name: ergonomic-service
  namespace: talentbridge
  labels: { app.kubernetes.io/name: ergonomic-service, talentbridge.io/domain: occupational-health }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50282, targetPort: 50282 }]
  selector: { app.kubernetes.io/name: ergonomic-service }
