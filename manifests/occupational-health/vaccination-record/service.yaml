apiVersion: v1
kind: Service
metadata:
  name: vaccination-record
  namespace: talentbridge
  labels: { app.kubernetes.io/name: vaccination-record, talentbridge.io/domain: occupational-health }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50283, targetPort: 50283 }]
  selector: { app.kubernetes.io/name: vaccination-record }
