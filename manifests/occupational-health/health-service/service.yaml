apiVersion: v1
kind: Service
metadata:
  name: health-service
  namespace: talentbridge
  labels: { app.kubernetes.io/name: health-service, talentbridge.io/domain: occupational-health }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50280, targetPort: 50280 }]
  selector: { app.kubernetes.io/name: health-service }
