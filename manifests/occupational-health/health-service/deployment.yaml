apiVersion: apps/v1
kind: Deployment
metadata:
  name: health-service
  namespace: talentbridge
  labels:
    app.kubernetes.io/name: health-service
    app.kubernetes.io/part-of: talentbridge
    talentbridge.io/domain: occupational-health
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: health-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: health-service, talentbridge.io/domain: occupational-health }
    spec:
      serviceAccountName: health-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: health-service
          image: harbor.talentbridge.internal/talentbridge/health-service:latest
          ports: [{ containerPort: 50280, name: http }]
          envFrom: [{ configMapRef: { name: health-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50280 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50280 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
