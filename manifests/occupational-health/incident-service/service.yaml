apiVersion: v1
kind: Service
metadata:
  name: incident-service
  namespace: talentbridge
  labels: { app.kubernetes.io/name: incident-service, talentbridge.io/domain: occupational-health }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50281, targetPort: 50281 }]
  selector: { app.kubernetes.io/name: incident-service }
