apiVersion: apps/v1
kind: Deployment
metadata:
  name: incident-service
  namespace: talentbridge
  labels:
    app.kubernetes.io/name: incident-service
    app.kubernetes.io/part-of: talentbridge
    talentbridge.io/domain: occupational-health
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: incident-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: incident-service, talentbridge.io/domain: occupational-health }
    spec:
      serviceAccountName: incident-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: incident-service
          image: harbor.talentbridge.internal/talentbridge/incident-service:latest
          ports: [{ containerPort: 50281, name: http }]
          envFrom: [{ configMapRef: { name: incident-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50281 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50281 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
