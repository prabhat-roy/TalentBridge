apiVersion: apps/v1
kind: Deployment
metadata:
  name: mfa-service
  namespace: talentbridge
  labels:
    app.kubernetes.io/name: mfa-service
    app.kubernetes.io/part-of: talentbridge
    talentbridge.io/domain: identity
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: mfa-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: mfa-service, talentbridge.io/domain: identity }
    spec:
      serviceAccountName: mfa-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: mfa-service
          image: harbor.talentbridge.internal/talentbridge/mfa-service:latest
          ports: [{ containerPort: 50022, name: http }]
          envFrom: [{ configMapRef: { name: mfa-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50022 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50022 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
