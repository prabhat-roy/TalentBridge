apiVersion: apps/v1
kind: Deployment
metadata:
  name: employee-sso
  namespace: talentbridge
  labels:
    app.kubernetes.io/name: employee-sso
    app.kubernetes.io/part-of: talentbridge
    talentbridge.io/domain: identity
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: employee-sso } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: employee-sso, talentbridge.io/domain: identity }
    spec:
      serviceAccountName: employee-sso
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: employee-sso
          image: harbor.talentbridge.internal/talentbridge/employee-sso:latest
          ports: [{ containerPort: 50021, name: http }]
          envFrom: [{ configMapRef: { name: employee-sso } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50021 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50021 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
