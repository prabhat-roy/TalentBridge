apiVersion: v1
kind: ConfigMap
metadata:
  name: provisioning-service
  namespace: talentbridge
  labels: { app.kubernetes.io/name: provisioning-service, talentbridge.io/domain: identity }
data:
  LOG_LEVEL: info
  PORT: "50024"
