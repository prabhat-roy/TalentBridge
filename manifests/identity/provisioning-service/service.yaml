apiVersion: v1
kind: Service
metadata:
  name: provisioning-service
  namespace: talentbridge
  labels: { app.kubernetes.io/name: provisioning-service, talentbridge.io/domain: identity }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50024, targetPort: 50024 }]
  selector: { app.kubernetes.io/name: provisioning-service }
