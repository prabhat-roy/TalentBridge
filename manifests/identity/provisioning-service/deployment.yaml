apiVersion: apps/v1
kind: Deployment
metadata:
  name: provisioning-service
  namespace: talentbridge
  labels:
    app.kubernetes.io/name: provisioning-service
    app.kubernetes.io/part-of: talentbridge
    talentbridge.io/domain: identity
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: provisioning-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: provisioning-service, talentbridge.io/domain: identity }
    spec:
      serviceAccountName: provisioning-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: provisioning-service
          image: harbor.talentbridge.internal/talentbridge/provisioning-service:latest
          ports: [{ containerPort: 50024, name: http }]
          envFrom: [{ configMapRef: { name: provisioning-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50024 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50024 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
