apiVersion: v1
kind: Service
metadata: { name: api-gateway, namespace: talentbridge, labels: { app.kubernetes.io/name: api-gateway, talentbridge.io/domain: platform } }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50000, targetPort: 50000 }]
  selector: { app.kubernetes.io/name: api-gateway }
