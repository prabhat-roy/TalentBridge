apiVersion: v1
kind: Service
metadata:
  name: file-upload-service
  namespace: talentbridge
  labels: { app.kubernetes.io/name: file-upload-service, talentbridge.io/domain: platform }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 51011, targetPort: 51011 }]
  selector: { app.kubernetes.io/name: file-upload-service }
