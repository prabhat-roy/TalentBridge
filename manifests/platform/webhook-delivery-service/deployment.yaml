apiVersion: apps/v1
kind: Deployment
metadata:
  name: webhook-delivery-service
  namespace: talentbridge
  labels:
    app.kubernetes.io/name: webhook-delivery-service
    app.kubernetes.io/part-of: talentbridge
    talentbridge.io/domain: platform
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: webhook-delivery-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: webhook-delivery-service, talentbridge.io/domain: platform }
    spec:
      serviceAccountName: webhook-delivery-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: webhook-delivery-service
          image: harbor.talentbridge.internal/talentbridge/webhook-delivery-service:latest
          ports: [{ containerPort: 51004, name: http }]
          envFrom: [{ configMapRef: { name: webhook-delivery-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 51004 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 51004 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
