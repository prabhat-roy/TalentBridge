apiVersion: v1
kind: Service
metadata:
  name: i18n-service
  namespace: talentbridge
  labels: { app.kubernetes.io/name: i18n-service, talentbridge.io/domain: platform }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 51012, targetPort: 51012 }]
  selector: { app.kubernetes.io/name: i18n-service }
