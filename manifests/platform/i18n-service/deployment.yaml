apiVersion: apps/v1
kind: Deployment
metadata:
  name: i18n-service
  namespace: talentbridge
  labels:
    app.kubernetes.io/name: i18n-service
    app.kubernetes.io/part-of: talentbridge
    talentbridge.io/domain: platform
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: i18n-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: i18n-service, talentbridge.io/domain: platform }
    spec:
      serviceAccountName: i18n-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: i18n-service
          image: harbor.talentbridge.internal/talentbridge/i18n-service:latest
          ports: [{ containerPort: 51012, name: http }]
          envFrom: [{ configMapRef: { name: i18n-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 51012 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 51012 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
