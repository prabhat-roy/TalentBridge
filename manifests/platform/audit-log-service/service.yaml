apiVersion: v1
kind: Service
metadata:
  name: audit-log-service
  namespace: talentbridge
  labels: { app.kubernetes.io/name: audit-log-service, talentbridge.io/domain: platform }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 51008, targetPort: 51008 }]
  selector: { app.kubernetes.io/name: audit-log-service }
