apiVersion: apps/v1
kind: Deployment
metadata:
  name: idempotency-service
  namespace: talentbridge
  labels:
    app.kubernetes.io/name: idempotency-service
    app.kubernetes.io/part-of: talentbridge
    talentbridge.io/domain: platform
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: idempotency-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: idempotency-service, talentbridge.io/domain: platform }
    spec:
      serviceAccountName: idempotency-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: idempotency-service
          image: harbor.talentbridge.internal/talentbridge/idempotency-service:latest
          ports: [{ containerPort: 51000, name: http }]
          envFrom: [{ configMapRef: { name: idempotency-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 51000 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 51000 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
