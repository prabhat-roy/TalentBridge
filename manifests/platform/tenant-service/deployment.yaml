apiVersion: apps/v1
kind: Deployment
metadata:
  name: tenant-service
  namespace: talentbridge
  labels:
    app.kubernetes.io/name: tenant-service
    app.kubernetes.io/part-of: talentbridge
    talentbridge.io/domain: platform
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: tenant-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: tenant-service, talentbridge.io/domain: platform }
    spec:
      serviceAccountName: tenant-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: tenant-service
          image: harbor.talentbridge.internal/talentbridge/tenant-service:latest
          ports: [{ containerPort: 51006, name: http }]
          envFrom: [{ configMapRef: { name: tenant-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 51006 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 51006 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
