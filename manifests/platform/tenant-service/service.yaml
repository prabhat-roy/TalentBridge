apiVersion: v1
kind: Service
metadata:
  name: tenant-service
  namespace: talentbridge
  labels: { app.kubernetes.io/name: tenant-service, talentbridge.io/domain: platform }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 51006, targetPort: 51006 }]
  selector: { app.kubernetes.io/name: tenant-service }
