apiVersion: v1
kind: Service
metadata:
  name: rate-limit-service
  namespace: talentbridge
  labels: { app.kubernetes.io/name: rate-limit-service, talentbridge.io/domain: platform }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 51007, targetPort: 51007 }]
  selector: { app.kubernetes.io/name: rate-limit-service }
