apiVersion: v1
kind: Service
metadata:
  name: notification-preferences-service
  namespace: talentbridge
  labels: { app.kubernetes.io/name: notification-preferences-service, talentbridge.io/domain: platform }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 51005, targetPort: 51005 }]
  selector: { app.kubernetes.io/name: notification-preferences-service }
