apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-preferences-service
  namespace: talentbridge
  labels:
    app.kubernetes.io/name: notification-preferences-service
    app.kubernetes.io/part-of: talentbridge
    talentbridge.io/domain: platform
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: notification-preferences-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: notification-preferences-service, talentbridge.io/domain: platform }
    spec:
      serviceAccountName: notification-preferences-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: notification-preferences-service
          image: harbor.talentbridge.internal/talentbridge/notification-preferences-service:latest
          ports: [{ containerPort: 51005, name: http }]
          envFrom: [{ configMapRef: { name: notification-preferences-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 51005 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 51005 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
