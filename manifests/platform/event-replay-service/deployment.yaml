apiVersion: apps/v1
kind: Deployment
metadata:
  name: event-replay-service
  namespace: talentbridge
  labels:
    app.kubernetes.io/name: event-replay-service
    app.kubernetes.io/part-of: talentbridge
    talentbridge.io/domain: platform
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: event-replay-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: event-replay-service, talentbridge.io/domain: platform }
    spec:
      serviceAccountName: event-replay-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: event-replay-service
          image: harbor.talentbridge.internal/talentbridge/event-replay-service:latest
          ports: [{ containerPort: 51009, name: http }]
          envFrom: [{ configMapRef: { name: event-replay-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 51009 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 51009 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
