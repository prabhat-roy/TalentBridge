apiVersion: apps/v1
kind: Deployment
metadata:
  name: dead-letter-service
  namespace: talentbridge
  labels:
    app.kubernetes.io/name: dead-letter-service
    app.kubernetes.io/part-of: talentbridge
    talentbridge.io/domain: platform
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: dead-letter-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: dead-letter-service, talentbridge.io/domain: platform }
    spec:
      serviceAccountName: dead-letter-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: dead-letter-service
          image: harbor.talentbridge.internal/talentbridge/dead-letter-service:latest
          ports: [{ containerPort: 51010, name: http }]
          envFrom: [{ configMapRef: { name: dead-letter-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 51010 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 51010 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
