apiVersion: v1
kind: Service
metadata:
  name: erp-integration
  namespace: talentbridge
  labels: { app.kubernetes.io/name: erp-integration, talentbridge.io/domain: integrations }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50320, targetPort: 50320 }]
  selector: { app.kubernetes.io/name: erp-integration }
