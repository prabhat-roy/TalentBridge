apiVersion: v1
kind: Service
metadata:
  name: background-service
  namespace: talentbridge
  labels: { app.kubernetes.io/name: background-service, talentbridge.io/domain: integrations }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50323, targetPort: 50323 }]
  selector: { app.kubernetes.io/name: background-service }
