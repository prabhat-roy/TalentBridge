apiVersion: v1
kind: Service
metadata:
  name: identity-provisioning
  namespace: talentbridge
  labels: { app.kubernetes.io/name: identity-provisioning, talentbridge.io/domain: integrations }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50322, targetPort: 50322 }]
  selector: { app.kubernetes.io/name: identity-provisioning }
