apiVersion: v1
kind: Service
metadata:
  name: finance-integration
  namespace: talentbridge
  labels: { app.kubernetes.io/name: finance-integration, talentbridge.io/domain: integrations }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50321, targetPort: 50321 }]
  selector: { app.kubernetes.io/name: finance-integration }
