apiVersion: apps/v1
kind: Deployment
metadata:
  name: headcount-service
  namespace: talentbridge
  labels:
    app.kubernetes.io/name: headcount-service
    app.kubernetes.io/part-of: talentbridge
    talentbridge.io/domain: workforce-planning
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: headcount-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: headcount-service, talentbridge.io/domain: workforce-planning }
    spec:
      serviceAccountName: headcount-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: headcount-service
          image: harbor.talentbridge.internal/talentbridge/headcount-service:latest
          ports: [{ containerPort: 50200, name: http }]
          envFrom: [{ configMapRef: { name: headcount-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50200 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50200 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
