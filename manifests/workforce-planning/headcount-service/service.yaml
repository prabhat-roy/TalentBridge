apiVersion: v1
kind: Service
metadata:
  name: headcount-service
  namespace: talentbridge
  labels: { app.kubernetes.io/name: headcount-service, talentbridge.io/domain: workforce-planning }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50200, targetPort: 50200 }]
  selector: { app.kubernetes.io/name: headcount-service }
