apiVersion: v1
kind: Service
metadata:
  name: forecast-service
  namespace: talentbridge
  labels: { app.kubernetes.io/name: forecast-service, talentbridge.io/domain: workforce-planning }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50201, targetPort: 50201 }]
  selector: { app.kubernetes.io/name: forecast-service }
