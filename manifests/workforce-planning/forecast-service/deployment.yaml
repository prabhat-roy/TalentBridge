apiVersion: apps/v1
kind: Deployment
metadata:
  name: forecast-service
  namespace: talentbridge
  labels:
    app.kubernetes.io/name: forecast-service
    app.kubernetes.io/part-of: talentbridge
    talentbridge.io/domain: workforce-planning
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: forecast-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: forecast-service, talentbridge.io/domain: workforce-planning }
    spec:
      serviceAccountName: forecast-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: forecast-service
          image: harbor.talentbridge.internal/talentbridge/forecast-service:latest
          ports: [{ containerPort: 50201, name: http }]
          envFrom: [{ configMapRef: { name: forecast-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50201 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50201 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
