apiVersion: v1
kind: Service
metadata:
  name: job-posting
  namespace: talentbridge
  labels: { app.kubernetes.io/name: job-posting, talentbridge.io/domain: recruitment }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50060, targetPort: 50060 }]
  selector: { app.kubernetes.io/name: job-posting }
