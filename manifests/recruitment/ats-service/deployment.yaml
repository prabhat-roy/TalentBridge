apiVersion: apps/v1
kind: Deployment
metadata:
  name: ats-service
  namespace: talentbridge
  labels:
    app.kubernetes.io/name: ats-service
    app.kubernetes.io/part-of: talentbridge
    talentbridge.io/domain: recruitment
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: ats-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: ats-service, talentbridge.io/domain: recruitment }
    spec:
      serviceAccountName: ats-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: ats-service
          image: harbor.talentbridge.internal/talentbridge/ats-service:latest
          ports: [{ containerPort: 50061, name: http }]
          envFrom: [{ configMapRef: { name: ats-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50061 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50061 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
