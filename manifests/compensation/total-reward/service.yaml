apiVersion: v1
kind: Service
metadata:
  name: total-reward
  namespace: talentbridge
  labels: { app.kubernetes.io/name: total-reward, talentbridge.io/domain: compensation }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50183, targetPort: 50183 }]
  selector: { app.kubernetes.io/name: total-reward }
