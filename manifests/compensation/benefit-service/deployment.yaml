apiVersion: apps/v1
kind: Deployment
metadata:
  name: benefit-service
  namespace: talentbridge
  labels:
    app.kubernetes.io/name: benefit-service
    app.kubernetes.io/part-of: talentbridge
    talentbridge.io/domain: compensation
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: benefit-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: benefit-service, talentbridge.io/domain: compensation }
    spec:
      serviceAccountName: benefit-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: benefit-service
          image: harbor.talentbridge.internal/talentbridge/benefit-service:latest
          ports: [{ containerPort: 50181, name: http }]
          envFrom: [{ configMapRef: { name: benefit-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50181 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50181 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
