apiVersion: v1
kind: Service
metadata:
  name: equity-service
  namespace: talentbridge
  labels: { app.kubernetes.io/name: equity-service, talentbridge.io/domain: compensation }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50182, targetPort: 50182 }]
  selector: { app.kubernetes.io/name: equity-service }
