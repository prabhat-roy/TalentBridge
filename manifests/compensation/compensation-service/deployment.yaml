apiVersion: apps/v1
kind: Deployment
metadata:
  name: compensation-service
  namespace: talentbridge
  labels:
    app.kubernetes.io/name: compensation-service
    app.kubernetes.io/part-of: talentbridge
    talentbridge.io/domain: compensation
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: compensation-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: compensation-service, talentbridge.io/domain: compensation }
    spec:
      serviceAccountName: compensation-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: compensation-service
          image: harbor.talentbridge.internal/talentbridge/compensation-service:latest
          ports: [{ containerPort: 50180, name: http }]
          envFrom: [{ configMapRef: { name: compensation-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50180 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50180 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
