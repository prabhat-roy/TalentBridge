apiVersion: v1
kind: Service
metadata:
  name: compensation-service
  namespace: talentbridge
  labels: { app.kubernetes.io/name: compensation-service, talentbridge.io/domain: compensation }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50180, targetPort: 50180 }]
  selector: { app.kubernetes.io/name: compensation-service }
