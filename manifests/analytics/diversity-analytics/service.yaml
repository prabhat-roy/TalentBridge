apiVersion: v1
kind: Service
metadata:
  name: diversity-analytics
  namespace: talentbridge
  labels: { app.kubernetes.io/name: diversity-analytics, talentbridge.io/domain: analytics }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50302, targetPort: 50302 }]
  selector: { app.kubernetes.io/name: diversity-analytics }
