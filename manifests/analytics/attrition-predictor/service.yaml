apiVersion: v1
kind: Service
metadata:
  name: attrition-predictor
  namespace: talentbridge
  labels: { app.kubernetes.io/name: attrition-predictor, talentbridge.io/domain: analytics }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50301, targetPort: 50301 }]
  selector: { app.kubernetes.io/name: attrition-predictor }
