apiVersion: apps/v1
kind: Deployment
metadata:
  name: people-analytics
  namespace: talentbridge
  labels:
    app.kubernetes.io/name: people-analytics
    app.kubernetes.io/part-of: talentbridge
    talentbridge.io/domain: analytics
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: people-analytics } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: people-analytics, talentbridge.io/domain: analytics }
    spec:
      serviceAccountName: people-analytics
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: people-analytics
          image: harbor.talentbridge.internal/talentbridge/people-analytics:latest
          ports: [{ containerPort: 50300, name: http }]
          envFrom: [{ configMapRef: { name: people-analytics } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50300 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50300 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
