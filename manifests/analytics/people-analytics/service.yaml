apiVersion: v1
kind: Service
metadata:
  name: people-analytics
  namespace: talentbridge
  labels: { app.kubernetes.io/name: people-analytics, talentbridge.io/domain: analytics }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50300, targetPort: 50300 }]
  selector: { app.kubernetes.io/name: people-analytics }
