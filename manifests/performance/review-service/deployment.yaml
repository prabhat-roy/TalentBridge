apiVersion: apps/v1
kind: Deployment
metadata:
  name: review-service
  namespace: talentbridge
  labels:
    app.kubernetes.io/name: review-service
    app.kubernetes.io/part-of: talentbridge
    talentbridge.io/domain: performance
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: review-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: review-service, talentbridge.io/domain: performance }
    spec:
      serviceAccountName: review-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: review-service
          image: harbor.talentbridge.internal/talentbridge/review-service:latest
          ports: [{ containerPort: 50141, name: http }]
          envFrom: [{ configMapRef: { name: review-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50141 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50141 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
