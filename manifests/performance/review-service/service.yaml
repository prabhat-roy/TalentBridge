apiVersion: v1
kind: Service
metadata:
  name: review-service
  namespace: talentbridge
  labels: { app.kubernetes.io/name: review-service, talentbridge.io/domain: performance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50141, targetPort: 50141 }]
  selector: { app.kubernetes.io/name: review-service }
