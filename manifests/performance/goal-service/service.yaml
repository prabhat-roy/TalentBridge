apiVersion: v1
kind: Service
metadata:
  name: goal-service
  namespace: talentbridge
  labels: { app.kubernetes.io/name: goal-service, talentbridge.io/domain: performance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50140, targetPort: 50140 }]
  selector: { app.kubernetes.io/name: goal-service }
