apiVersion: apps/v1
kind: Deployment
metadata:
  name: goal-service
  namespace: talentbridge
  labels:
    app.kubernetes.io/name: goal-service
    app.kubernetes.io/part-of: talentbridge
    talentbridge.io/domain: performance
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: goal-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: goal-service, talentbridge.io/domain: performance }
    spec:
      serviceAccountName: goal-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: goal-service
          image: harbor.talentbridge.internal/talentbridge/goal-service:latest
          ports: [{ containerPort: 50140, name: http }]
          envFrom: [{ configMapRef: { name: goal-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50140 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50140 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
