apiVersion: apps/v1
kind: Deployment
metadata:
  name: feedback-service
  namespace: talentbridge
  labels:
    app.kubernetes.io/name: feedback-service
    app.kubernetes.io/part-of: talentbridge
    talentbridge.io/domain: performance
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: feedback-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: feedback-service, talentbridge.io/domain: performance }
    spec:
      serviceAccountName: feedback-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: feedback-service
          image: harbor.talentbridge.internal/talentbridge/feedback-service:latest
          ports: [{ containerPort: 50142, name: http }]
          envFrom: [{ configMapRef: { name: feedback-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50142 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50142 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
