apiVersion: v1
kind: Service
metadata:
  name: feedback-service
  namespace: talentbridge
  labels: { app.kubernetes.io/name: feedback-service, talentbridge.io/domain: performance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50142, targetPort: 50142 }]
  selector: { app.kubernetes.io/name: feedback-service }
