apiVersion: v1
kind: Service
metadata:
  name: okr-service
  namespace: talentbridge
  labels: { app.kubernetes.io/name: okr-service, talentbridge.io/domain: performance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50144, targetPort: 50144 }]
  selector: { app.kubernetes.io/name: okr-service }
