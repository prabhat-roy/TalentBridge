apiVersion: apps/v1
kind: Deployment
metadata:
  name: okr-service
  namespace: talentbridge
  labels:
    app.kubernetes.io/name: okr-service
    app.kubernetes.io/part-of: talentbridge
    talentbridge.io/domain: performance
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: okr-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: okr-service, talentbridge.io/domain: performance }
    spec:
      serviceAccountName: okr-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: okr-service
          image: harbor.talentbridge.internal/talentbridge/okr-service:latest
          ports: [{ containerPort: 50144, name: http }]
          envFrom: [{ configMapRef: { name: okr-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50144 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50144 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
