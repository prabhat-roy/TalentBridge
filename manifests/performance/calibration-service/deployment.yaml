apiVersion: apps/v1
kind: Deployment
metadata:
  name: calibration-service
  namespace: talentbridge
  labels:
    app.kubernetes.io/name: calibration-service
    app.kubernetes.io/part-of: talentbridge
    talentbridge.io/domain: performance
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: calibration-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: calibration-service, talentbridge.io/domain: performance }
    spec:
      serviceAccountName: calibration-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: calibration-service
          image: harbor.talentbridge.internal/talentbridge/calibration-service:latest
          ports: [{ containerPort: 50143, name: http }]
          envFrom: [{ configMapRef: { name: calibration-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50143 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50143 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
