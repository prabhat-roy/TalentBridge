apiVersion: v1
kind: Service
metadata:
  name: calibration-service
  namespace: talentbridge
  labels: { app.kubernetes.io/name: calibration-service, talentbridge.io/domain: performance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50143, targetPort: 50143 }]
  selector: { app.kubernetes.io/name: calibration-service }
