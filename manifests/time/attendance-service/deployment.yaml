apiVersion: apps/v1
kind: Deployment
metadata:
  name: attendance-service
  namespace: talentbridge
  labels:
    app.kubernetes.io/name: attendance-service
    app.kubernetes.io/part-of: talentbridge
    talentbridge.io/domain: time
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: attendance-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: attendance-service, talentbridge.io/domain: time }
    spec:
      serviceAccountName: attendance-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: attendance-service
          image: harbor.talentbridge.internal/talentbridge/attendance-service:latest
          ports: [{ containerPort: 50120, name: http }]
          envFrom: [{ configMapRef: { name: attendance-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50120 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50120 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
