apiVersion: v1
kind: Service
metadata:
  name: attendance-service
  namespace: talentbridge
  labels: { app.kubernetes.io/name: attendance-service, talentbridge.io/domain: time }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50120, targetPort: 50120 }]
  selector: { app.kubernetes.io/name: attendance-service }
