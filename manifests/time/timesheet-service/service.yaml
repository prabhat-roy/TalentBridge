apiVersion: v1
kind: Service
metadata:
  name: timesheet-service
  namespace: talentbridge
  labels: { app.kubernetes.io/name: timesheet-service, talentbridge.io/domain: time }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50124, targetPort: 50124 }]
  selector: { app.kubernetes.io/name: timesheet-service }
