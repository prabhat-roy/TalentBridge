apiVersion: apps/v1
kind: Deployment
metadata:
  name: timesheet-service
  namespace: talentbridge
  labels:
    app.kubernetes.io/name: timesheet-service
    app.kubernetes.io/part-of: talentbridge
    talentbridge.io/domain: time
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: timesheet-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: timesheet-service, talentbridge.io/domain: time }
    spec:
      serviceAccountName: timesheet-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: timesheet-service
          image: harbor.talentbridge.internal/talentbridge/timesheet-service:latest
          ports: [{ containerPort: 50124, name: http }]
          envFrom: [{ configMapRef: { name: timesheet-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50124 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50124 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
