apiVersion: v1
kind: Service
metadata:
  name: overtime-service
  namespace: talentbridge
  labels: { app.kubernetes.io/name: overtime-service, talentbridge.io/domain: time }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50123, targetPort: 50123 }]
  selector: { app.kubernetes.io/name: overtime-service }
