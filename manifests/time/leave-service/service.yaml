apiVersion: v1
kind: Service
metadata:
  name: leave-service
  namespace: talentbridge
  labels: { app.kubernetes.io/name: leave-service, talentbridge.io/domain: time }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50122, targetPort: 50122 }]
  selector: { app.kubernetes.io/name: leave-service }
