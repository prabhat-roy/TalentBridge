apiVersion: v1
kind: Service
metadata:
  name: shift-service
  namespace: talentbridge
  labels: { app.kubernetes.io/name: shift-service, talentbridge.io/domain: time }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50121, targetPort: 50121 }]
  selector: { app.kubernetes.io/name: shift-service }
