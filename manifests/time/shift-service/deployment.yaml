apiVersion: apps/v1
kind: Deployment
metadata:
  name: shift-service
  namespace: talentbridge
  labels:
    app.kubernetes.io/name: shift-service
    app.kubernetes.io/part-of: talentbridge
    talentbridge.io/domain: time
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: shift-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: shift-service, talentbridge.io/domain: time }
    spec:
      serviceAccountName: shift-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: shift-service
          image: harbor.talentbridge.internal/talentbridge/shift-service:latest
          ports: [{ containerPort: 50121, name: http }]
          envFrom: [{ configMapRef: { name: shift-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50121 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50121 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
