apiVersion: v1
kind: Service
metadata:
  name: grievance-service
  namespace: talentbridge
  labels: { app.kubernetes.io/name: grievance-service, talentbridge.io/domain: compliance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50263, targetPort: 50263 }]
  selector: { app.kubernetes.io/name: grievance-service }
