apiVersion: apps/v1
kind: Deployment
metadata:
  name: visa-service
  namespace: talentbridge
  labels:
    app.kubernetes.io/name: visa-service
    app.kubernetes.io/part-of: talentbridge
    talentbridge.io/domain: compliance
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: visa-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: visa-service, talentbridge.io/domain: compliance }
    spec:
      serviceAccountName: visa-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: visa-service
          image: harbor.talentbridge.internal/talentbridge/visa-service:latest
          ports: [{ containerPort: 50261, name: http }]
          envFrom: [{ configMapRef: { name: visa-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50261 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50261 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
