apiVersion: v1
kind: Service
metadata:
  name: visa-service
  namespace: talentbridge
  labels: { app.kubernetes.io/name: visa-service, talentbridge.io/domain: compliance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50261, targetPort: 50261 }]
  selector: { app.kubernetes.io/name: visa-service }
