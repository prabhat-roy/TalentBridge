apiVersion: v1
kind: Service
metadata:
  name: labour-compliance
  namespace: talentbridge
  labels: { app.kubernetes.io/name: labour-compliance, talentbridge.io/domain: compliance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50260, targetPort: 50260 }]
  selector: { app.kubernetes.io/name: labour-compliance }
