apiVersion: apps/v1
kind: Deployment
metadata:
  name: policy-service
  namespace: talentbridge
  labels:
    app.kubernetes.io/name: policy-service
    app.kubernetes.io/part-of: talentbridge
    talentbridge.io/domain: compliance
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: policy-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: policy-service, talentbridge.io/domain: compliance }
    spec:
      serviceAccountName: policy-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: policy-service
          image: harbor.talentbridge.internal/talentbridge/policy-service:latest
          ports: [{ containerPort: 50262, name: http }]
          envFrom: [{ configMapRef: { name: policy-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50262 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50262 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
