apiVersion: v1
kind: Service
metadata:
  name: policy-service
  namespace: talentbridge
  labels: { app.kubernetes.io/name: policy-service, talentbridge.io/domain: compliance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50262, targetPort: 50262 }]
  selector: { app.kubernetes.io/name: policy-service }
