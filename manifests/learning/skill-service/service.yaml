apiVersion: v1
kind: Service
metadata:
  name: skill-service
  namespace: talentbridge
  labels: { app.kubernetes.io/name: skill-service, talentbridge.io/domain: learning }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50163, targetPort: 50163 }]
  selector: { app.kubernetes.io/name: skill-service }
