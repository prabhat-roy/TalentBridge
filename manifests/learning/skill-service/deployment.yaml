apiVersion: apps/v1
kind: Deployment
metadata:
  name: skill-service
  namespace: talentbridge
  labels:
    app.kubernetes.io/name: skill-service
    app.kubernetes.io/part-of: talentbridge
    talentbridge.io/domain: learning
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: skill-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: skill-service, talentbridge.io/domain: learning }
    spec:
      serviceAccountName: skill-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: skill-service
          image: harbor.talentbridge.internal/talentbridge/skill-service:latest
          ports: [{ containerPort: 50163, name: http }]
          envFrom: [{ configMapRef: { name: skill-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50163 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50163 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
