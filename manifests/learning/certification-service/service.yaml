apiVersion: v1
kind: Service
metadata:
  name: certification-service
  namespace: talentbridge
  labels: { app.kubernetes.io/name: certification-service, talentbridge.io/domain: learning }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50162, targetPort: 50162 }]
  selector: { app.kubernetes.io/name: certification-service }
