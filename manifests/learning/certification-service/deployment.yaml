apiVersion: apps/v1
kind: Deployment
metadata:
  name: certification-service
  namespace: talentbridge
  labels:
    app.kubernetes.io/name: certification-service
    app.kubernetes.io/part-of: talentbridge
    talentbridge.io/domain: learning
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: certification-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: certification-service, talentbridge.io/domain: learning }
    spec:
      serviceAccountName: certification-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: certification-service
          image: harbor.talentbridge.internal/talentbridge/certification-service:latest
          ports: [{ containerPort: 50162, name: http }]
          envFrom: [{ configMapRef: { name: certification-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50162 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50162 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
