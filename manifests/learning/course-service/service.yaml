apiVersion: v1
kind: Service
metadata:
  name: course-service
  namespace: talentbridge
  labels: { app.kubernetes.io/name: course-service, talentbridge.io/domain: learning }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50161, targetPort: 50161 }]
  selector: { app.kubernetes.io/name: course-service }
