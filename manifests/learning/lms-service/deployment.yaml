apiVersion: apps/v1
kind: Deployment
metadata:
  name: lms-service
  namespace: talentbridge
  labels:
    app.kubernetes.io/name: lms-service
    app.kubernetes.io/part-of: talentbridge
    talentbridge.io/domain: learning
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: lms-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: lms-service, talentbridge.io/domain: learning }
    spec:
      serviceAccountName: lms-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: lms-service
          image: harbor.talentbridge.internal/talentbridge/lms-service:latest
          ports: [{ containerPort: 50160, name: http }]
          envFrom: [{ configMapRef: { name: lms-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50160 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50160 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
