apiVersion: v1
kind: Service
metadata:
  name: lms-service
  namespace: talentbridge
  labels: { app.kubernetes.io/name: lms-service, talentbridge.io/domain: learning }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50160, targetPort: 50160 }]
  selector: { app.kubernetes.io/name: lms-service }
