apiVersion: v1
kind: Service
metadata:
  name: idp-service
  namespace: talentbridge
  labels: { app.kubernetes.io/name: idp-service, talentbridge.io/domain: learning }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50164, targetPort: 50164 }]
  selector: { app.kubernetes.io/name: idp-service }
