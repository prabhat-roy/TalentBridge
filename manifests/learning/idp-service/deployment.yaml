apiVersion: apps/v1
kind: Deployment
metadata:
  name: idp-service
  namespace: talentbridge
  labels:
    app.kubernetes.io/name: idp-service
    app.kubernetes.io/part-of: talentbridge
    talentbridge.io/domain: learning
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: idp-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: idp-service, talentbridge.io/domain: learning }
    spec:
      serviceAccountName: idp-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: idp-service
          image: harbor.talentbridge.internal/talentbridge/idp-service:latest
          ports: [{ containerPort: 50164, name: http }]
          envFrom: [{ configMapRef: { name: idp-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50164 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50164 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
