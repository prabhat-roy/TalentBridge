# tax-service

Per-country tax engine. Pluggable country modules (IN/US/UK/EU/UAE/SG).
Country code is mandatory on every tax call (enforced by OPA policy `tax-engine-per-country-required.rego`).

**Domain:** `payroll` · **Language:** java · **Port:** 50042
