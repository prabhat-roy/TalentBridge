image: { repository: greenbone/openvas-scanner, tag: latest }
gvmd: { enabled: true }
gsa:
  enabled: true
  ingress:
    enabled: true
    hosts:
      - { host: openvas.talentbridge.internal }
persistence:
  storageClass: gp3
  size: 100Gi
