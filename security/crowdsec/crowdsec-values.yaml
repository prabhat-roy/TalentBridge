agent:
  enabled: true
  acquisition:
    - { namespace: talentbridge, podName: api-gateway-* }
lapi:
  enabled: true
  dashboard: { enabled: true }
bouncers:
  - { name: crowdsec-traefik-bouncer, image: crowdsecurity/traefik-bouncer:latest }
