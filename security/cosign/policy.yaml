apiVersion: policy.sigstore.dev/v1beta1
kind: ClusterImagePolicy
metadata: { name: talentbridge-cosign }
spec:
  images:
    - glob: "harbor.talentbridge.internal/talentbridge/*"
  authorities:
    - name: keyless-github
      keyless:
        url: https://fulcio.sigstore.dev
        identities:
          - issuer: https://token.actions.githubusercontent.com
            subjectRegExp: 'https://github.com/prabhat-roy/TalentBridge/.*'
