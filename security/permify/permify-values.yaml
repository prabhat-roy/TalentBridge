image: { repository: ghcr.io/permify/permify, tag: latest }
database:
  engine: postgres
  uri: { secretKeyRef: { name: permify-postgres, key: uri } }
ingress:
  enabled: true
  hosts: [{ host: permify.talentbridge.internal }]
