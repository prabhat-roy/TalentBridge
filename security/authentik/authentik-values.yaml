image: { repository: ghcr.io/goauthentik/server, tag: 2024.10.4 }
authentik: { secret_key: change-me }
postgresql: { enabled: true }
redis: { enabled: true }
server:
  ingress:
    enabled: true
    hosts: [{ host: auth.talentbridge.internal }]
