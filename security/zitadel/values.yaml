# ZITADEL — developer-platform auth (API keys, OAuth clients, dev portal SSO)
image: { repository: ghcr.io/zitadel/zitadel, tag: latest }
zitadel:
  externalDomain: zitadel.talentbridge.internal
  externalSecure: true
  database: { postgres: { host: postgres.talentbridge.svc, database: zitadel } }
ingress: { enabled: true, hosts: [{ host: zitadel.talentbridge.internal }] }
