apiVersion: batch/v1
kind: Job
metadata: { name: kube-hunter, namespace: talentbridge-security }
spec:
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: kube-hunter
          image: aquasec/kube-hunter:latest
          command: ["kube-hunter","--pod","--report","yaml"]
