apiVersion: litmuschaos.io/v1alpha1
kind: ChaosEngine
metadata: { name: talentbridge-node-drain, namespace: litmus }
spec:
  appinfo: { appns: talentbridge, applabel: "tier=api", appkind: deployment }
  chaosServiceAccount: litmus-admin
  experiments:
    - { name: node-drain, spec: { components: { env: [ { name: TOTAL_CHAOS_DURATION, value: "60" } ] } } }
