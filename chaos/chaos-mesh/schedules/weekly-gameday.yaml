apiVersion: chaos-mesh.org/v1alpha1
kind: Schedule
metadata: { name: talentbridge-weekly-gameday, namespace: chaos-mesh }
spec:
  schedule: "0 9 * * 6"
  type: Workflow
  workflow:
    entry: gameday-entry
