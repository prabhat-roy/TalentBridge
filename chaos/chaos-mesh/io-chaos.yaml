apiVersion: chaos-mesh.org/v1alpha1
kind: IOChaos
metadata: { name: talentbridge-io-latency, namespace: chaos-mesh }
spec:
  action: latency
  mode: one
  selector: { namespaces: [talentbridge] }
  volumePath: /var/lib/data
  delay: "100ms"
  percent: 50
  duration: 5m
