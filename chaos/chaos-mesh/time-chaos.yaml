apiVersion: chaos-mesh.org/v1alpha1
kind: TimeChaos
metadata: { name: talentbridge-clock-skew, namespace: chaos-mesh }
spec:
  mode: one
  selector: { namespaces: [talentbridge] }
  timeOffset: "+500ms"
  duration: 3m
