# TalentBridge — local development stack
# `make compose-up` (or `docker compose up`) brings up the full data-plane.

version: "3.9"

services:
  postgres:
    image: postgres:16
    container_name: talentbridge-postgres
    environment: { POSTGRES_USER: app, POSTGRES_PASSWORD: app, POSTGRES_DB: app }
    ports: ["5432:5432"]
    volumes: ["postgres-data:/var/lib/postgresql/data"]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U app"]
      interval: 5s
      timeout: 3s
      retries: 10

  mongodb:
    image: mongo:8.0
    container_name: talentbridge-mongo
    ports: ["27017:27017"]
    volumes: ["mongo-data:/data/db"]

  redis:
    image: redis:7-alpine
    container_name: talentbridge-redis
    ports: ["6379:6379"]
    command: ["redis-server", "--appendonly", "yes"]
    volumes: ["redis-data:/data"]

  zookeeper:
    image: confluentinc/cp-zookeeper:7.7.1
    container_name: talentbridge-zookeeper
    environment: { ZOOKEEPER_CLIENT_PORT: 2181, ZOOKEEPER_TICK_TIME: 2000 }

  kafka:
    image: confluentinc/cp-kafka:7.7.1
    container_name: talentbridge-kafka
    depends_on: [zookeeper]
    ports: ["9092:9092"]
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:29092,PLAINTEXT_HOST://localhost:9092
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT
      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1

  schema-registry:
    image: confluentinc/cp-schema-registry:7.7.1
    container_name: talentbridge-schema-registry
    depends_on: [kafka]
    ports: ["8081:8081"]
    environment:
      SCHEMA_REGISTRY_HOST_NAME: schema-registry
      SCHEMA_REGISTRY_KAFKASTORE_BOOTSTRAP_SERVERS: kafka:29092

  minio:
    image: minio/minio:latest
    container_name: talentbridge-minio
    command: ["server", "/data", "--console-address", ":9001"]
    environment: { MINIO_ROOT_USER: minioadmin, MINIO_ROOT_PASSWORD: minioadmin }
    ports: ["9000:9000", "9001:9001"]
    volumes: ["minio-data:/data"]

  keycloak:
    image: quay.io/keycloak/keycloak:25.0
    container_name: talentbridge-keycloak
    command: ["start-dev"]
    environment: { KEYCLOAK_ADMIN: admin, KEYCLOAK_ADMIN_PASSWORD: admin }
    ports: ["8080:8080"]

  vault:
    image: hashicorp/vault:1.17
    container_name: talentbridge-vault
    cap_add: [IPC_LOCK]
    environment: { VAULT_DEV_ROOT_TOKEN_ID: root, VAULT_DEV_LISTEN_ADDRESS: "0.0.0.0:8200" }
    ports: ["8200:8200"]

  prometheus:
    image: prom/prometheus:v2.55.0
    container_name: talentbridge-prometheus
    ports: ["9090:9090"]

  grafana:
    image: grafana/grafana:11.2.0
    container_name: talentbridge-grafana
    ports: ["3001:3000"]
    environment: { GF_SECURITY_ADMIN_PASSWORD: admin }

  loki:
    image: grafana/loki:3.2.0
    container_name: talentbridge-loki
    ports: ["3100:3100"]

  jaeger:
    image: jaegertracing/all-in-one:1.61
    container_name: talentbridge-jaeger
    ports: ["16686:16686", "4317:4317", "4318:4318"]

  neo4j:
    image: neo4j:5.23
    container_name: talentbridge-neo4j
    environment: { NEO4J_AUTH: "neo4j/talentbridge" }
    ports: ["7474:7474", "7687:7687"]
    volumes: ["neo4j-data:/data"]

  cassandra:
    image: cassandra:5.0
    container_name: talentbridge-cassandra
    ports: ["9042:9042"]
    volumes: ["cassandra-data:/var/lib/cassandra"]

  rabbitmq:
    image: rabbitmq:3.13-management
    container_name: talentbridge-rabbit
    ports: ["5672:5672", "15672:15672"]

volumes:
  postgres-data:
  mongo-data:
  redis-data:
  minio-data:
  neo4j-data:
  cassandra-data:
