apiVersion: v1
kind: Service
metadata:
  name: {{ include "knowledge-transfer.fullname" . }}
  labels: {{- include "knowledge-transfer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "knowledge-transfer.selectorLabels" . | nindent 4 }}
