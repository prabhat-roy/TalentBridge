apiVersion: v2
name: payroll-service
description: Payroll engine — saga-orchestrated multi-country runs (BigDecimal money, blue-green only)
type: application
version: 0.1.0
appVersion: "0.1.0"
dependencies: [{ name: lib-talentbridge, version: 0.1.0, repository: file://../_lib-talentbridge }]
