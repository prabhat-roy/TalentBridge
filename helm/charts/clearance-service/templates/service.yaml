apiVersion: v1
kind: Service
metadata:
  name: {{ include "clearance-service.fullname" . }}
  labels: {{- include "clearance-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "clearance-service.selectorLabels" . | nindent 4 }}
