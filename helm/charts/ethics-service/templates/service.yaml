apiVersion: v1
kind: Service
metadata:
  name: {{ include "ethics-service.fullname" . }}
  labels: {{- include "ethics-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "ethics-service.selectorLabels" . | nindent 4 }}
