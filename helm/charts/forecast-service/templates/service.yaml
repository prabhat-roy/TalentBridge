apiVersion: v1
kind: Service
metadata:
  name: {{ include "forecast-service.fullname" . }}
  labels: {{- include "forecast-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "forecast-service.selectorLabels" . | nindent 4 }}
