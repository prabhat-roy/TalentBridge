apiVersion: v1
kind: Service
metadata:
  name: {{ include "erp-integration.fullname" . }}
  labels: {{- include "erp-integration.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "erp-integration.selectorLabels" . | nindent 4 }}
