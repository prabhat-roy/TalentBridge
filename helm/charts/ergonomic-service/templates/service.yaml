apiVersion: v1
kind: Service
metadata:
  name: {{ include "ergonomic-service.fullname" . }}
  labels: {{- include "ergonomic-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "ergonomic-service.selectorLabels" . | nindent 4 }}
