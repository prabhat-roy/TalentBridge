apiVersion: v1
kind: Service
metadata:
  name: {{ include "alumni-service.fullname" . }}
  labels: {{- include "alumni-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "alumni-service.selectorLabels" . | nindent 4 }}
