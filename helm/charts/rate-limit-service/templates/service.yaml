apiVersion: v1
kind: Service
metadata:
  name: {{ include "rate-limit-service.fullname" . }}
  labels: {{- include "rate-limit-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "rate-limit-service.selectorLabels" . | nindent 4 }}
