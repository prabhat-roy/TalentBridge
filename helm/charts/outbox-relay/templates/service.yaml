apiVersion: v1
kind: Service
metadata:
  name: {{ include "outbox-relay.fullname" . }}
  labels: {{- include "outbox-relay.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "outbox-relay.selectorLabels" . | nindent 4 }}
