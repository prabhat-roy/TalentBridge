apiVersion: v1
kind: Service
metadata:
  name: {{ include "skill-service.fullname" . }}
  labels: {{- include "skill-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "skill-service.selectorLabels" . | nindent 4 }}
