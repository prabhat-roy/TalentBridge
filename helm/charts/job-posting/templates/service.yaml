apiVersion: v1
kind: Service
metadata:
  name: {{ include "job-posting.fullname" . }}
  labels: {{- include "job-posting.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "job-posting.selectorLabels" . | nindent 4 }}
