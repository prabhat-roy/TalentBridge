apiVersion: v1
kind: Service
metadata:
  name: {{ include "api-key-service.fullname" . }}
  labels: {{- include "api-key-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "api-key-service.selectorLabels" . | nindent 4 }}
