apiVersion: v1
kind: Service
metadata:
  name: {{ include "lms-service.fullname" . }}
  labels: {{- include "lms-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "lms-service.selectorLabels" . | nindent 4 }}
