apiVersion: v1
kind: Service
metadata:
  name: {{ include "attrition-predictor.fullname" . }}
  labels: {{- include "attrition-predictor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "attrition-predictor.selectorLabels" . | nindent 4 }}
