apiVersion: v1
kind: Service
metadata:
  name: {{ include "audit-log-service.fullname" . }}
  labels: {{- include "audit-log-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "audit-log-service.selectorLabels" . | nindent 4 }}
