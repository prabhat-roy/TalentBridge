apiVersion: v1
kind: Service
metadata:
  name: {{ include "diversity-analytics.fullname" . }}
  labels: {{- include "diversity-analytics.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "diversity-analytics.selectorLabels" . | nindent 4 }}
