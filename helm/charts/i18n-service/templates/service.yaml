apiVersion: v1
kind: Service
metadata:
  name: {{ include "i18n-service.fullname" . }}
  labels: {{- include "i18n-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "i18n-service.selectorLabels" . | nindent 4 }}
