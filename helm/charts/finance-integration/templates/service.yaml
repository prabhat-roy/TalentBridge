apiVersion: v1
kind: Service
metadata:
  name: {{ include "finance-integration.fullname" . }}
  labels: {{- include "finance-integration.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "finance-integration.selectorLabels" . | nindent 4 }}
