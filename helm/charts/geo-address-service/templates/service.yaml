apiVersion: v1
kind: Service
metadata:
  name: {{ include "geo-address-service.fullname" . }}
  labels: {{- include "geo-address-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "geo-address-service.selectorLabels" . | nindent 4 }}
