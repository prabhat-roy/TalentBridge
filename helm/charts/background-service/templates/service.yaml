apiVersion: v1
kind: Service
metadata:
  name: {{ include "background-service.fullname" . }}
  labels: {{- include "background-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "background-service.selectorLabels" . | nindent 4 }}
