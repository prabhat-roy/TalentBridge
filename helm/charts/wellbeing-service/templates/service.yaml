apiVersion: v1
kind: Service
metadata:
  name: {{ include "wellbeing-service.fullname" . }}
  labels: {{- include "wellbeing-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "wellbeing-service.selectorLabels" . | nindent 4 }}
