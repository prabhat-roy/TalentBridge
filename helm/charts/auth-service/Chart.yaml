apiVersion: v2
name: auth-service
description: OAuth2/OIDC + SAML2 federation gateway for TalentBridge
type: application
version: 0.1.0
appVersion: "0.1.0"
dependencies: [{ name: lib-talentbridge, version: 0.1.0, repository: file://../_lib-talentbridge }]
