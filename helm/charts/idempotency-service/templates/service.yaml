apiVersion: v1
kind: Service
metadata:
  name: {{ include "idempotency-service.fullname" . }}
  labels: {{- include "idempotency-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "idempotency-service.selectorLabels" . | nindent 4 }}
