apiVersion: v1
kind: Service
metadata:
  name: {{ include "applicant-tracking.fullname" . }}
  labels: {{- include "applicant-tracking.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "applicant-tracking.selectorLabels" . | nindent 4 }}
