apiVersion: v1
kind: Service
metadata:
  name: {{ include "total-reward.fullname" . }}
  labels: {{- include "total-reward.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "total-reward.selectorLabels" . | nindent 4 }}
