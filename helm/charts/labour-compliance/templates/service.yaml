apiVersion: v1
kind: Service
metadata:
  name: {{ include "labour-compliance.fullname" . }}
  labels: {{- include "labour-compliance.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "labour-compliance.selectorLabels" . | nindent 4 }}
