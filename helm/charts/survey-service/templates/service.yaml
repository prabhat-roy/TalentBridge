apiVersion: v1
kind: Service
metadata:
  name: {{ include "survey-service.fullname" . }}
  labels: {{- include "survey-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "survey-service.selectorLabels" . | nindent 4 }}
