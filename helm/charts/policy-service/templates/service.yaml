apiVersion: v1
kind: Service
metadata:
  name: {{ include "policy-service.fullname" . }}
  labels: {{- include "policy-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "policy-service.selectorLabels" . | nindent 4 }}
