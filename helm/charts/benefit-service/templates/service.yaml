apiVersion: v1
kind: Service
metadata:
  name: {{ include "benefit-service.fullname" . }}
  labels: {{- include "benefit-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "benefit-service.selectorLabels" . | nindent 4 }}
