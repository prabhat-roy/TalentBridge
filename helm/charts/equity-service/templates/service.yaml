apiVersion: v1
kind: Service
metadata:
  name: {{ include "equity-service.fullname" . }}
  labels: {{- include "equity-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "equity-service.selectorLabels" . | nindent 4 }}
