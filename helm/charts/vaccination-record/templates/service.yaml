apiVersion: v1
kind: Service
metadata:
  name: {{ include "vaccination-record.fullname" . }}
  labels: {{- include "vaccination-record.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "vaccination-record.selectorLabels" . | nindent 4 }}
