apiVersion: v1
kind: Service
metadata:
  name: {{ include "succession-service.fullname" . }}
  labels: {{- include "succession-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "succession-service.selectorLabels" . | nindent 4 }}
