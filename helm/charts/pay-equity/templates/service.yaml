apiVersion: v1
kind: Service
metadata:
  name: {{ include "pay-equity.fullname" . }}
  labels: {{- include "pay-equity.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "pay-equity.selectorLabels" . | nindent 4 }}
