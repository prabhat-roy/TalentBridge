apiVersion: v1
kind: Service
metadata:
  name: {{ include "saga-orchestrator.fullname" . }}
  labels: {{- include "saga-orchestrator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "saga-orchestrator.selectorLabels" . | nindent 4 }}
