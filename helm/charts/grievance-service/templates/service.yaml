apiVersion: v1
kind: Service
metadata:
  name: {{ include "grievance-service.fullname" . }}
  labels: {{- include "grievance-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "grievance-service.selectorLabels" . | nindent 4 }}
