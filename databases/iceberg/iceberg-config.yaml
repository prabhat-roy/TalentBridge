catalog:
  name: talentbridge_lake
  type: rest
  uri: https://nessie.talentbridge.internal/iceberg
warehouse: s3://talentbridge-data-lake/warehouse
io:
  s3:
    endpoint: https://s3.amazonaws.com
    region: us-east-1
table-default:
  write.format.default: parquet
  write.metadata.compression-codec: gzip
