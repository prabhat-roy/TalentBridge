apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata: { name: allow-istio-mesh, namespace: talentbridge }
spec:
  podSelector: {}
  policyTypes: [Ingress]
  ingress:
    - from: [{ namespaceSelector: { matchLabels: { istio-injection: enabled } } }]
