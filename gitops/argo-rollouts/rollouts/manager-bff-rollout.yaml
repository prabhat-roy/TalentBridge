apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata: { name: manager-bff, namespace: talentbridge }
spec:
  replicas: 3
  strategy: { canary: { steps: [{ setWeight: 10 }, { pause: { duration: 5m } }, { setWeight: 50 }, { pause: { duration: 5m } }, { setWeight: 100 }] } }
  selector: { matchLabels: { app.kubernetes.io/name: manager-bff } }
  template:
    metadata: { labels: { app.kubernetes.io/name: manager-bff } }
    spec:
      containers: [{ name: manager-bff, image: harbor.talentbridge.internal/talentbridge/manager-bff, ports: [{ containerPort: 50002 }] }]
