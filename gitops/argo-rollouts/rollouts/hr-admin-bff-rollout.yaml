apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata: { name: hr-admin-bff, namespace: talentbridge }
spec:
  replicas: 3
  strategy: { canary: { steps: [{ setWeight: 10 }, { pause: { duration: 5m } }, { setWeight: 50 }, { pause: { duration: 5m } }, { setWeight: 100 }] } }
  selector: { matchLabels: { app.kubernetes.io/name: hr-admin-bff } }
  template:
    metadata: { labels: { app.kubernetes.io/name: hr-admin-bff } }
    spec:
      containers: [{ name: hr-admin-bff, image: harbor.talentbridge.internal/talentbridge/hr-admin-bff, ports: [{ containerPort: 50003 }] }]
