apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata: { name: talentbridge, namespace: flux-system }
spec:
  interval: 1m
  url: https://github.com/prabhat-roy/TalentBridge.git
  ref: { branch: main }
