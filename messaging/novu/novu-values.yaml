global:
  novuVersion: 1.0.0
api: { ingress: { enabled: true, hosts: [{ host: novu-api.talentbridge.internal }] } }
web: { ingress: { enabled: true, hosts: [{ host: novu.talentbridge.internal }] } }
mongodb: { enabled: true }
redis:   { enabled: true }
