# in-toto layout — supply-chain step attestations for TalentBridge
expires: "2027-01-01T00:00:00Z"
keys: { project-key: { keyid: talentbridge-key, keytype: ed25519, scheme: ed25519 } }
steps:
  - { name: source, expected_command: ["git", "clone"], pubkeys: [talentbridge-key] }
  - { name: build, expected_command: ["docker", "build"], pubkeys: [talentbridge-key] }
  - { name: test, expected_command: ["make", "test"], pubkeys: [talentbridge-key] }
inspect:
  - { name: signed-image, run: ["cosign", "verify"], expected_materials: [], expected_products: [] }
