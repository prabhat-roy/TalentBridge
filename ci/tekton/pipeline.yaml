apiVersion: tekton.dev/v1
kind: Pipeline
metadata: { name: talentbridge-deploy }
spec:
  params:
    - { name: cloud,   type: string }
    - { name: region,  type: string, description: "eu | in | us — residency region" }
    - { name: env,     type: string }
    - { name: service, type: string }
    - { name: version, type: string }
  workspaces:
    - { name: source }
    - { name: kubeconfig }
  tasks:
    - name: helm-upgrade
      taskRef: { name: helm-upgrade-from-source }
      workspaces:
        - { name: source,     workspace: source }
        - { name: kubeconfig, workspace: kubeconfig }
      params:
        - { name: charts_dir,        value: "helm/charts/$(params.service)" }
        - { name: namespace,         value: talentbridge }
        - { name: release_name,      value: "$(params.service)" }
        - { name: extra_values_files, value: "values.yaml,values-$(params.cloud).yaml" }
        - { name: extra_set,         value: "region=$(params.region)" }
