opencost:
  prometheus: { internal: { enabled: false }, external: { url: http://prometheus.observability:9090 } }
  ui: { enabled: true }
  exporter:
    cloudProviderApiKey: ""
    defaultClusterId: talentbridge-prod
ingress:
  enabled: true
  hosts: [{ host: opencost.talentbridge.internal, paths: [{ path: /, pathType: Prefix }] }]
